# hotspot-proxy-toggle config 예시

NETWORK_SERVICE='Wi-Fi'
WIFI_DEVICE=''

# 필수. 현재 release에서는 socks5만 지원함.
PROXY_TYPE=socks5

# 필수. 휴대폰이 핫스팟 라우터 IP 위에서 SOCKS5 proxy를 여는 port.
PROXY_PORT=1080

# 선택. 정확히 매칭할 SSID allow-list. 쉼표로 구분함.
HOTSPOT_SSIDS=''

# Android hotspot DHCP packet에는 보통 ANDROID_METERED가 포함됨.
# STRICT_SSID가 1이 아닐 때만 사용함.
HOTSPOT_DHCP_MARKERS='ANDROID_METERED'
STRICT_SSID=0

# macOS proxy setting을 켜기 전에 SOCKS5 no-auth handshake를 요구함.
REQUIRE_PROXY_CHECK=1
PROXY_CHECK_TIMEOUT=1
